/** Shared constants for the marketing pages. */

export const REPO_URL = "https://github.com/shriyansnaik/minion-ai";
export const PYPI_URL = "https://pypi.org/project/minion-ai";
export const DOCS_ENTRY = "/getting-started/what-is-minion/";

/**
 * Demo video. Recorded last, deliberately, so it shows the finished pre-launch
 * product (see ROADMAP.md). Set this to the embed URL — e.g.
 * "https://www.youtube.com/embed/XXXXXXXXXXX" — and the hero swaps the
 * placeholder for the real player with no other change.
 */
export const DEMO_VIDEO_EMBED_URL: string | null = null;
